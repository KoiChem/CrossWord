# 有機化学クロスワード学習アプリ 実装仕様書

## 1. プロジェクト概要

高校化学の「有機化合物」を対象にした、ブラウザで動作するクロスワード型学習アプリを開発する。

単なる用語暗記ではなく、クロスワードの交差によって
- 既知の文字から答えを推測する
- 物質名・構造・性質・反応を相互に想起する
- 関連する有機化学知識をひとまとまりとして学ぶ

ことを狙う。

最終的なテーマは以下の4つを想定する。

1. 脂肪族
2. 芳香族
3. 天然高分子
4. 合成高分子

ただし、初期実装は **脂肪族のみ** とする。

---

## 2. 初期版の対象範囲

脂肪族のうち、高校化学で扱う以下を中心とする。

- 脂肪族炭化水素
  - アルカン
  - アルケン
  - アルキン
- アルコール
- エーテル
- アルデヒド
- ケトン
- カルボン酸
- エステル
- 油脂・セッケン
- 上記に関連する代表的な反応・検出反応・異性体概念

初期開発では本番用語データを最初から大量投入せず、20〜30語程度の仮データでGeneratorとPlayerを成立させる。

本番版では脂肪族60〜80語程度を目安に拡張する。

---

## 3. 盤面上の解答表記

### 3.1 基本方針

盤面上の答えは **ひらがな** とする。

例：

- エタノール → `えたのーる`
- アセトアルデヒド → `あせとあるでひど`
- 酢酸 → `さくさん`
- 銀鏡反応 → `ぎんきょうはんのう`

### 3.2 許可文字

解答として許可する文字は以下。

- ひらがな
- 小書きひらがな
  - ぁぃぅぇぉ
  - ゃゅょ
  - っ
  - ゎ
- ゔ
- 長音符 `ー`

盤面では小書き文字、長音符ともに1マスとして扱う。

### 3.3 入力制御

ユーザー入力は可能な限り「ひらがな＋長音符」のみを受け付ける。

ただしWebアプリからOSの日本語入力モード自体を強制することはできないため、IME変換中の文字列を直接拒否しないこと。

IMEの `compositionstart` / `compositionend` を考慮し、変換確定後に正規化・検証する。

入力値について以下を実施する。

- 全角/半角の正規化
- カタカナ入力は可能ならひらがなへ自動変換
- 英数字・漢字・不要な記号は盤面に確定させない
- 文字入力時に次のマスへ自然に移動する
- Backspaceで前のマスへ戻れる
- タテ/ヨコ交差セルの選択が自然に切り替わる

1マスごとに独立した `<input>` を大量配置するより、IME処理が安定する構成を優先する。必要なら単一の隠しinput/textareaで文字入力を受け、選択中セルへ反映する方式を検討する。

---

## 4. UI / UX 方針

### 4.1 全体

「教材アプリ」よりも「質の良いパズルゲーム」に寄せる。

目安：
- パズルゲーム感 85%
- 化学らしさ 15%

背景に分子構造や試験管を大量配置するような、過剰な“理科教材感”は避ける。

### 4.2 画面構成

初期版では以下を想定。

#### Home
- タイトル
- テーマ表示：脂肪族
- 難易度選択
- 盤面サイズ選択
- START
- SE ON/OFF
- 必要に応じて簡単な遊び方

#### Puzzle
- クロスワード盤面
- タテ/ヨコ問題一覧または現在の問題表示
- 選択中の問題を明確に表示
- 入力中の単語を強調
- ヒント
- クリア進捗
- リセット/再生成
- SE ON/OFF

#### Result
- COMPLETE表示
- 正答数
- 所要時間（実装コストが低ければ）
- 新しいパズルを作る
- 同難易度でもう一度

### 4.3 レスポンシブ

PC、iPad、スマートフォンで快適に動作すること。

特にスマートフォンでは
- 盤面が画面幅に収まる
- 最低限のタップ領域を確保
- ヒントが読める
- ソフトウェアキーボード表示時でも選択セルが隠れにくい

ことを重視する。

---

## 5. 難易度

ユーザーに見せる難易度はシンプルにする。

初期案：

| 難易度 | 盤面 | 語数目安 |
|---|---:|---:|
| MINI | 7×7 | 4〜5 |
| EASY | 9×9 | 5〜7 |
| NORMAL | 11×11 | 7〜9 |
| HARD | 13×13 | 9〜11 |
| MASTER | 15×15 | 11〜14 |

ただし初期版は EASY / NORMAL / HARD の3段階でもよい。

内部的には以下でも難易度を調整する。

- 盤面サイズ
- 出題語数
- ヒント難度
- 交差率
- 長い単語の割合

長さが盤面サイズを超える単語は候補から除外する。

---

## 6. 用語データモデル

用語データとアプリロジックは必ず分離する。

例：

```js
{
  id: "ethanol",
  displayName: "エタノール",
  answer: "えたのーる",
  theme: "aliphatic",
  category: "alcohol",
  family: "alcohol_name",
  difficulty: 1,
  priority: 3,
  clues: [
    {
      level: 1,
      type: "formula",
      text: "分子式 C2H6O の代表的な一価アルコール"
    },
    {
      level: 2,
      type: "reaction",
      text: "酸化するとアセトアルデヒドを生じる物質"
    },
    {
      level: 3,
      type: "synthesis",
      text: "エチレンに水を付加して得られる物質"
    }
  ],
  related: [
    { id: "ethylene", weight: 3 },
    { id: "acetaldehyde", weight: 3 },
    { id: "acetic_acid", weight: 3 },
    { id: "ester", weight: 2 }
  ]
}
```

### 必須候補フィールド

- `id`
- `displayName`
- `answer`
- `theme`
- `category`
- `family`
- `difficulty`
- `priority`
- `clues`
- `related`

### family の目的

似すぎた語が同時に大量出題されるのを防ぐ。

例：
- `あるかん`
- `あるけん`
- `あるきん`

や

- `めたのーる`
- `えたのーる`

が同じパズルに偏りすぎないようにする。

原則として同一 `family` からの採用数に上限を設ける。

---

## 7. 出題ヒント

1つの用語に複数ヒントを持たせる。

ヒントは以下の観点から用意可能な構造にする。

1. 名称・分類
2. 構造式・分子式
3. 性質
4. 反応
5. 合成法
6. 関連物質との関係

同じ答えでも難易度によりヒントを切り替える。

例：答え `さくさん`

EASY:
- `CH3COOH で表されるカルボン酸の名称`

NORMAL:
- `エタノールを十分に酸化すると生成するカルボン酸`

HARD:
- `アセトアルデヒドを酸化して得られる物質`

---

## 8. パズル生成の基本思想

完全ランダムで単語を抜き出して配置するだけにはしない。

以下の手順を基本とする。

1. 中心概念を1つ選ぶ
2. 関連語データから関連度の高い語を集める
3. 同テーマから一部ランダム語を混ぜる
4. 盤面サイズと単語長で候補を絞る
5. family重複を抑える
6. 複数の配置候補を生成する
7. 各候補をスコアリングする
8. 最良の盤面を採用する

候補語の目安：

- 70%：中心概念と関連の深い語
- 30%：同テーマ内のランダム語

比率は固定ではなく、品質を見ながら調整可能とする。

---

## 9. クロスワード配置アルゴリズム

初期実装では、以下を候補とする。

- 文字交差候補の事前計算
- backtracking
- MRV（配置候補の少ない語を優先）
- random restart
- 複数配置生成
- スコアリングによる最良配置選択

過度に複雑なSAT/CSPライブラリ等は、必要性が確認されるまでは導入しない。

完全な新聞型クロスワードのように全マスを埋める必要はない。

本アプリでは、化学用語だけで構成される「疎なクロスワード」を許容する。

### 配置時の基本制約

- 交差する文字は一致すること
- 不自然な隣接によって別単語に見えないこと
- 単語同士が無意味に接触しないこと
- 原則として全単語が1つの連結成分を形成すること
- 盤面の極端な偏りを避ける
- 長い1語に短い語が一方向から大量に刺さるような配置を減点する

---

## 10. 配置スコア

「配置できたか」だけでなく「パズルとして気持ちよいか」を評価する。

例：

```text
score =
  + placedWordCount
  + crossingCount
  + compactness
  + connectedness
  + semanticRelatedness
  + intersectionQuality
  - isolatedWordPenalty
  - edgeBiasPenalty
  - awkwardAdjacencyPenalty
  - familyBiasPenalty
```

具体的な重みは実装・テストを通じて調整する。

### intersectionQuality

交差文字の質も評価対象とする。

例：
- 比較的特徴的な文字で交差 → 高評価
- `ん` `う` `る` など頻出文字だけの交差 → 低評価
- 長音符 `ー` の交差 → 原則低評価または減点

文字頻度データを厳密に作る必要はなく、初期版は簡単なヒューリスティックでよい。

---

## 11. 単語長の扱い

クロスワードとして不向きな語を制御する。

目安：

- 2文字：原則除外
- 3〜8文字：主力
- 9〜11文字：適度に採用
- 12文字以上：大きい盤面を中心に採用

`priority` を用いて重要度とパズル適性を分けて扱えるようにする。

---

## 12. SE / VFX

パズルとしての操作感を高めるため、軽量なSEとVFXを入れる。

### SE例

- 1文字入力：小さなtick
- 1単語正解：軽いclick/chime
- 交差による連鎖完成：少し豪華な音
- 全問正解：短いcomplete音

Web Audio APIで生成できる程度の軽量SEを優先する。

### VFX例

- 正解単語が一瞬わずかに拡大
- 正解した文字列に光が走る
- 間違い時に数px程度のshake
- COMPLETE時に盤面へ光が走る
- 軽量な粒子演出

過剰な演出や重いアニメーションは避ける。

`prefers-reduced-motion` を尊重する。

---

## 13. 正誤判定

初期版では以下を基本とする。

- 入力中に1文字ごとの不正解表示を強く出さない
- 単語が埋まった時点で判定可能
- 正解した単語は固定または視覚的に確定表示
- 間違い時は答えを即表示しない
- 必要なら「チェック」ボタン方式も検討

学習体験とパズル体験のバランスを優先する。

---

## 14. 技術構成

基本要件：

- 静的Webアプリ
- GitHub Pagesで公開可能
- サーバー必須構成にしない
- 不要な外部依存を増やさない
- 初期版はVanilla HTML/CSS/JavaScriptを第一候補とする
- React等を採用する場合は明確なメリットを説明すること

推奨構成例：

```text
/
├── index.html
├── AGENTS.md
├── SPEC.md
├── src/
│   ├── app.js
│   ├── generator/
│   │   ├── crossword-generator.js
│   │   ├── scorer.js
│   │   └── validator.js
│   ├── player/
│   │   ├── input-controller.js
│   │   └── puzzle-state.js
│   ├── ui/
│   │   └── renderer.js
│   └── audio/
│       └── sound.js
├── data/
│   └── aliphatic.js
├── styles/
│   └── main.css
└── tests/
```

実装しながらより合理的な構成が見つかった場合は改善してよい。

---

## 15. テスト要件

最低限以下を検証する。

### Generator

- 100回以上連続生成して例外が出ない
- 空盤面にならない
- 単語同士の衝突がない
- 不正な隣接がない
- 原則として配置単語が連結している
- 指定盤面サイズを超えない
- 同一familyに偏りすぎない
- 配置失敗時に再試行できる

可能なら1000回程度の自動生成テストを行う。

### Player

- PC Chrome
- PC Safari相当環境
- iPhone Safari
- iPad Safari
- Android Chrome相当

特に以下を確認する。

- 日本語IME
- ひらがな
- カタカナ→ひらがな変換
- 長音符
- 小書きかな
- Backspace
- タテ/ヨコ切替
- 交差セル

---

## 16. 初期開発フェーズ

### Phase 1: Generator

仮データ20〜30語。

実装：
- データ読み込み
- 単語候補抽出
- 交差候補計算
- 配置
- backtracking
- random restart
- スコアリング
- 最良盤面選択
- 検証

この段階ではUIは最小限でよい。

### Phase 2: Player

- 盤面表示
- セル選択
- タテ/ヨコ選択
- ヒント表示
- ひらがな入力
- IME対応
- 正誤判定
- 完成判定

### Phase 3: UI/UX

- レスポンシブ
- ホーム画面
- 難易度
- SE
- VFX
- Result画面

### Phase 4: 本番教材データ

脂肪族60〜80語程度へ拡張。

各語に
- 正式名称
- ひらがな解答
- category
- family
- difficulty
- priority
- 複数ヒント
- related

を付与する。

### Phase 5: QA / 最適化

- 大量生成テスト
- UIテスト
- モバイルIME
- パフォーマンス
- GitHub Pages公開

---

## 17. 将来機能

初期版では実装を必須としない。

### 17.1 他テーマ

- 芳香族
- 天然高分子
- 合成高分子

データファイル追加で拡張できる構造にする。

### 17.2 キーワード問題

特定マスの文字を拾って最後のキーワードを完成させる。

初期Generatorへ強い制約として組み込むのではなく、

1. 通常盤面生成
2. キーワードを構成可能か後処理で探索
3. 不可能なら別配置

という方式を第一候補とする。

### 17.3 学習履歴

localStorage等を利用し、

- correctCount
- wrongCount
- lastSeen

などを記録。

苦手語を次回少し出やすくする。

### 17.4 Knowledge Map

クリア後に、その回で出題された関連物質・反応のつながりを簡潔に表示。

例：

```text
えちれん
  ↓ 水の付加
えたのーる
  ↓ 酸化
あせとあるでひど
  ↓ 酸化
さくさん
```

---

## 18. 初期版で「やらないこと」

以下は初期実装の必須要件ではない。

- アカウント機能
- サーバー
- クラウドDB
- ランキング
- SNS共有
- AIによるリアルタイム問題生成
- 完全な全面埋めクロスワード
- キーワード問題
- Knowledge Map
- 本格的SRS
- オンライン対戦
- 重い3D/VFX

まず「毎回気持ちよい脂肪族クロスワードが生成され、スマホでも快適に解ける」ことを完成させる。

---

## 19. 成功条件

初期版の成功条件は以下。

1. 毎回異なる脂肪族クロスワードを生成できる
2. 配置が破綻せず、パズルとして自然
3. 化学的に妥当なヒントから解答できる
4. ひらがな入力がPC・スマートフォンで気持ちよく動く
5. 正解時のフィードバックが楽しい
6. GitHub Pagesで軽快に動作する
7. 将来3テーマを追加しやすい

最重要なのは、アルゴリズムの技巧ではなく

**「化学知識の関連性」と「クロスワードとしての楽しさ」が両立していること**

である。

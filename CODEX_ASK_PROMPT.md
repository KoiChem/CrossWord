# Codex 初回Ask Mode用プロンプト

このリポジトリの開発に入る前に、まず `SPEC.md` と `AGENTS.md` を最初から最後まで読んでください。

**この段階ではまだ実装・ファイル変更・コミット・pushをしないでください。**

高校化学「有機化合物」のクロスワード学習Webアプリを新規開発します。
最終的には「脂肪族・芳香族・天然高分子・合成高分子」の4テーマを想定していますが、初期実装は **脂肪族のみ** です。

以下について、実装前レビューをしてください。

## 1. 仕様レビュー

`SPEC.md` と `AGENTS.md` を読み、

- 矛盾している仕様
- 曖昧な仕様
- 技術的に危険な仕様
- 実装コストに対して効果が薄い仕様
- 高校化学学習アプリとして不足している重要要件

があれば指摘してください。

問題がない項目は無理に変更案を出さなくて構いません。

---

## 2. 推奨アーキテクチャ

この用途に対して、

- Vanilla HTML/CSS/JavaScript
- React等のフレームワーク

のどちらが適切か比較し、推奨案を示してください。

評価軸：

- GitHub Pagesとの相性
- bundle size
- 実装の複雑さ
- 日本語IME
- クロスワード盤面操作
- 保守性
- 将来4テーマへ拡張する場合
- モバイル動作

フレームワークを推奨する場合は「なぜVanilla JSでは不足するのか」を具体的に説明してください。

---

## 3. 推奨ファイル構成

実際に開発しやすいディレクトリ構成を提案してください。

特に以下を分離してください。

- Crossword Generator
- 配置Scorer / Validator
- Player state
- 日本語IME入力制御
- UI rendering
- SE
- 教材データ

将来

- aromatic
- natural-polymer
- synthetic-polymer

のデータを追加しやすい構成にしてください。

---

## 4. Crossword Generator設計

今回の最重要部分です。

以下を満たす生成アルゴリズムを設計してください。

- 毎回異なる盤面
- 盤面サイズ制約
- 単語数制約
- 原則として全単語が連結
- 十分な交差
- 不自然な隣接禁止
- 盤面の極端な偏りを避ける
- 同一familyの出題偏りを抑える
- 意味的に関連する語をある程度まとめる
- 交差文字の質も評価する
- 生成に失敗した場合は再試行
- スマホでも十分高速

候補として

- crossing候補の事前計算
- backtracking
- MRV
- random restart
- 複数候補生成
- score-based selection

を考えています。

それぞれ今回の用途に本当に必要か検討してください。

以下も提案してください。

- 盤面の内部表現
- 単語配置候補の表現
- 探索停止条件
- random seedの扱い
- 再現可能なデバッグ方法
- Generatorが固まらないための上限
- 盤面品質スコア

---

## 5. 日本語IME入力設計

盤面の答えは

- ひらがな
- 小書きひらがな
- ゔ
- 長音符 `ー`

のみです。

以下を満たす入力方式を提案してください。

- iPhone Safari
- iPad Safari
- macOS Safari
- Chrome系
- 日本語IME
- composition対応
- カタカナ入力時のひらがな正規化
- Backspace
- タテ/ヨコ切替
- 交差セル
- 1文字入力後の自動移動

1セル1input方式と、単一のhidden input/textarea方式を比較し、今回どちらが適切か判断してください。

---

## 6. 教材データモデル

`SPEC.md` のデータモデルを確認し、

- `id`
- `displayName`
- `answer`
- `theme`
- `category`
- `family`
- `difficulty`
- `priority`
- `clues`
- `related`

で十分か評価してください。

不足フィールドがあれば追加案を出してください。

ただし、過剰設計は避けてください。

---

## 7. テスト設計

Generatorを大量生成して品質を検証できるテストを設計してください。

最低限、

- 100回生成
- 1000回生成
- 配置語数
- 交差数
- 連結性
- 盤面外配置
- 衝突
- 不正隣接
- family偏り
- 生成時間

を確認したいです。

「Generatorとして正しい」だけでなく、「パズルとして気持ちよい」をある程度定量化する方法も提案してください。

---

## 8. 実装フェーズ

以下を叩き台として、より良い分割があれば修正してください。

### Phase 1
Crossword Generator

### Phase 2
Crossword Player / 日本語入力

### Phase 3
UI / UX / SE / VFX

### Phase 4
脂肪族の本番教材データ

### Phase 5
QA / optimization / GitHub Pages

各Phaseについて

- 目的
- 実装内容
- 完了条件
- テスト項目

を簡潔に示してください。

---

## 9. 初回実装でやらない方がよいもの

以下は将来機能です。

- キーワード問題
- Knowledge Map
- 学習履歴
- 芳香族
- 天然高分子
- 合成高分子

もし初期設計段階で将来拡張のために用意すべき最低限の抽象化があれば示してください。

ただし「将来使うかもしれない」という理由だけの過剰な抽象化は禁止です。

---

## 10. 最終回答形式

以下の順番で回答してください。

1. 仕様上の重要な指摘
2. 推奨アーキテクチャ
3. 推奨ファイル構成
4. Generator設計
5. 日本語IME設計
6. データモデル
7. テスト戦略
8. 実装フェーズ
9. 実装前に決めておくべきこと
10. Phase 1で最初に作るべき具体的なファイル一覧

最後に、

**「この設計でPhase 1へ進めてよいか」**

という判断材料として、採用案と代替案の差が分かるようにまとめてください。

繰り返しますが、今回は **Ask Modeでの設計レビューのみ** とし、まだコードは変更しないでください。
